#include <stdio.h>
/**
 * main - prints the phrase "programming is like building a multillingual puzzle
 * Return: 0 if exited properly, non-zero otherwise
 */
int main(void)
{
	puts("\"programmin is like building a multilligual puzzle");
	return (0);
}
